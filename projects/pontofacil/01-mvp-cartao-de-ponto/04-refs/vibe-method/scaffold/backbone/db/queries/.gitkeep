# Database Queries

Adicione queries especificas do seu dominio aqui.

Exemplo de estrutura:
- users.ts      # Queries de usuarios
- messages.ts   # Queries de mensagens
- etc.

Cada arquivo deve exportar funcoes que usam os helpers de db/index.ts:

```typescript
import { query, queryOne, insert, updateById } from '../index.js';

export async function findByEmail(email: string) {
  return queryOne('SELECT * FROM users WHERE email = $1', [email]);
}
```
