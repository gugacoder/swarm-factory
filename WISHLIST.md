# Wishlist

Itens desejados mas não prioritários.

## Budget de custo por sessão

Controle direto de custo por sessão via `max_cost_usd` no agent config. O `max_turns` é um proxy mas não é controle direto. Um circuit breaker real — se uma feature está consumindo $50 em tokens, algo está errado.
